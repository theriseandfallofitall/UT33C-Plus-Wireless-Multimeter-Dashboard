# Research Scripts

Focused one-off and repeatable experiments used during protocol discovery.

These scripts are intentionally more specialized than the tools in `tools/`. They are kept to preserve the reverse-engineering trail and to make future experiments reproducible.

Prefer the maintained host tools for normal use:

```bash
python -m tools.final_logger
python -m tools.pico_rig_runner --help
```
